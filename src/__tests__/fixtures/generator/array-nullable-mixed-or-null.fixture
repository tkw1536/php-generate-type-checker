---
input: "(array<string|null,mixed>)|null"
output: function
---
/** @phpstan-assert-if-true array<null|string, mixed>|null $value */
function isNullTypeOrArrayNullTypeOrStringToMixedType(mixed $value): bool
{
    return $value === null || isArrayNullTypeOrStringToMixedType($value);
}


/** @phpstan-assert-if-true array<null|string, mixed> $value */
function isArrayNullTypeOrStringToMixedType(mixed $value): bool
{
    if (!is_array($value)) {
        return false;
    }
    foreach ($value as $key1 => $value1) {
        if (!(is_string($key1) || $key1 === null)) {
            return false;
        }
    }
    return true;
}

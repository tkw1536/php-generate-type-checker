---
input: "(array<string|null,mixed>)|null"
---
<?php
    if ($data === null) {
        return true;
    }
    if (is_array($data)) {
        foreach ($data as $key => $value) {
            if (!((is_string($key) || $key === null))) {
                return false;
            }
        }
        return true;
    }
    return false;

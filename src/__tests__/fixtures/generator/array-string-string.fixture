---
input: "array<string, string>"
---
<?php
    if (!is_array($data)) {
        return false;
    }
    foreach ($data as $key => $value) {
        if (!(is_string($key))) {
            return false;
        }
        if (!(is_string($value))) {
            return false;
        }
    }
    return true;

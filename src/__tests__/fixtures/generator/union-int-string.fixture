---
input: "int|string"
output: function
---
<?php

/**
 * @param mixed $data
 * @phpstan-assert-if-true int|string $data
 */
function checkType(mixed $data): bool
{
    if (is_int($data)) {
        return true;
    }
    if (is_string($data)) {
        return true;
    }
    return false;
}

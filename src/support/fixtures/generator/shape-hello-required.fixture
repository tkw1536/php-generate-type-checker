---
input: "array{hello: string}"
output: function
---
/** @phpstan-assert-if-true array{hello: string} $value */
function isArrayHelloString(mixed $value): bool
{
    return (is_array($value) && array_key_exists('hello', $value) && is_string($value['hello']));
}
